#include<stdio.h>
#include<string.h>
void main()
{
    char s[30],ch,freq[30];
    int l;
    printf("Enter string:");
    scanf("%s",s);
    l=strlen(s);
    for(int i=0;s[i]!='\0';i++)
    {
        
    }
}